"""
پالت رنگی بخش چت و پشتیبانی (با پشتیبانی از دارک‌مود)
========================================================
به‌جای رنگ‌های ثابت، از get_colors() استفاده کن تا خودکار بین
حالت روشن و تاریک سوییچ کنه. فقط ACCENT (آبی اصلی) تو هر
دو حالت یکسان می‌مونه.
"""
from kivymd.app import MDApp

# آبی و سبز اصلی اپ (دقیقاً همون رنگی که تو کل اپ برای نوار بالا استفاده می‌شه)
ACCENT = (0.059, 0.239, 0.361, 1)      # #0F3D5C (آبی اصلی اپ)
ACCENT_GREEN = (0.122, 0.373, 0.290, 1)      # #1F5F4A (سبز، برای دکمه‌ی ارسال و تیک‌ها)
ACCENT_SOFT = (0.059, 0.239, 0.361, 0.18)  # همون آبی ولی کم‌رنگ (آواتار)
TEXT_ON_ACCENT = (1, 1, 1, 1)
BUBBLE_USER = ACCENT

# ---------- حالت روشن ----------
_LIGHT = {
    "BACKGROUND": (0.925, 0.91, 0.965, 1),      # #ECE8F6
    "SURFACE": (0.885, 0.86, 0.945, 1),         # #E2DBF1
    "BUBBLE_SUPPORT": (0.965, 0.955, 0.985, 1), # #F6F4FB
    "TEXT_DARK": (0.20, 0.15, 0.28, 1),
    "TEXT_MUTED": (0.50, 0.46, 0.58, 1),
    "DIVIDER": (0.82, 0.78, 0.88, 1),
}

# ---------- حالت تاریک ----------
_DARK = {
    "BACKGROUND": (0.055, 0.05, 0.065, 1),      # تقریباً مشکی، ته‌رنگ بنفش
    "SURFACE": (0.10, 0.09, 0.12, 1),
    "BUBBLE_SUPPORT": (0.17, 0.155, 0.20, 1),
    "TEXT_DARK": (0.93, 0.91, 0.96, 1),
    "TEXT_MUTED": (0.62, 0.58, 0.68, 1),
    "DIVIDER": (0.24, 0.22, 0.28, 1),
}


def get_colors():
    """
    رنگ‌های فعلی رو بر اساس دارک/لایت‌مود اپ برمی‌گردونه (یه دیکشنری).
    همه‌جا به‌جای رنگ ثابت از get_colors()["BACKGROUND"] و امثالش استفاده کن.
    """
    app = MDApp.get_running_app()
    is_dark = bool(app) and app.theme_cls.theme_style == "Dark"
    return _DARK if is_dark else _LIGHT